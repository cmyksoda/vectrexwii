#ifndef __CREDITS_H
#define __CREDITS_H

/* Credit-roll lines, defined in the generated credits.c. Each entry is one
 * scroll line: "" is a blank spacer, a line ending in ':' is a section header
 * (drawn in the title green), anything else is a name (drawn white). */
extern const char *credits_lines[];
extern const int credits_count;

#endif
