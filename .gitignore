# Object files
*.o
*.ko

# Libraries
*.lib
*.a

# Shared objects (inc. Windows DLLs)
*.dll
*.so
*.so.*
*.dylib

# Executables
*.exe
*.out
*.app

# Build output (regenerated by `make`; the .dol/.elf are linked from these)
build/
*.elf
*.map
*.dol

# GRRLIB + dependency tree --see the README for the deps needed to build.
portlibs-local/

# Local ROM/credits generators -- not needed to build
tools/
